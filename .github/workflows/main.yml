name: schedule Job

on:
  schedule:
    - cron: "0 0 * * *"

jobs:
  run-script:
    runs-on: macos-latest

    steps:
      - name: Print Message 
        run:  echo "Schedule Workflow execute"
      

  
